<Window x:Class="DGScopeProfileManager.Views.ProfileConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profile Configuration" Height="200" Width="500"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,15" FontWeight="Bold" FontSize="14">
            Confirm Profile Generation
        </TextBlock>

        <TextBlock Grid.Row="1" Margin="0,0,0,10" TextWrapping="Wrap">
            <Run Text="Facility: " FontWeight="Bold"/>
            <Run Name="FacilityName" Foreground="Blue"/>
        </TextBlock>

        <Border Grid.Row="2" Background="LightYellow" BorderBrush="Gold"
                BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock TextWrapping="Wrap" FontSize="11">
                    The following will be configured automatically:<LineBreak/>
                    <LineBreak/>
                    • Altimeter stations from CRC facility configuration<LineBreak/>
                    • NEXRAD station based on proximity to facility<LineBreak/>
                    • Home location, receivers, and video map
                </TextBlock>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Generate" IsDefault="True" Click="Ok_Click" MinWidth="80" Margin="5"/>
            <Button Content="Cancel" IsCancel="True" MinWidth="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
